The options you actually have
Option A — Session memory (what your AI suggested)

User logs in:

email + password

App temporarily remembers the password and auto-unlocks vault.

Pros:

no double typing

easy fix

Cons:

password exists in memory briefly

But guess what…

Every password manager does this.

Even Bitwarden keeps it in memory for the session.